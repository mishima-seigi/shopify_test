{{ "sticky-hero.css" | asset_url | stylesheet_tag }}

<div>
    <div class="sticky-hero__media" aria-hidden="true">
        {% for block in section.blocks %}
            {{ block.settings.block2-img | image_url: width: 1100, height: 500 | image_tag: class: "sticky-img" }}
        {% endfor %}
    </div>
    <div class="sticky-hero__content">
        {% for block in section.blocks %}    
            {{ block.settings.block2-text }}
        {% endfor %}
    </div>
</div>

{% schema %}
  {
    "name": "stucky-hero",
    "settings": [] ,
    "blocks": [
      {
        "name": "画像",
        "type": "block2",
        "limit": 1,
        "settings": [
          {
            "type": "image_picker",
            "id": "block2-img",
            "label": "画像を選択"
          },
          {
            "type": "textarea",
            "id": "block2-text",
            "label": "テキスト"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "sticky-hero"
      }
    ]
  }
{% endschema %}